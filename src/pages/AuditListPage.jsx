import { NavBar } from 'antd-mobile';

const AuditListPage = () => {
  return (
    <div>
      <NavBar back={null}>审核列表</NavBar>
      <p style={{ textAlign: 'center', padding: '16px' }}>这里将显示申请列表。</p>
    </div>
  );
};

export default AuditListPage;
